'use babel';

import AtomExtensionTizensdkView from '../lib/atom-extension-tizensdk-view';

describe('AtomExtensionTizensdkView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
